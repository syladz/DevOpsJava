plugins {
    id 'java'
    id 'checkstyle'
}

repositories {
    mavenCentral() // Add this repository to resolve Checkstyle
}


checkstyle {
    configFile = file('checkstyle.xml')
    ignoreFailures = false
    classpath = files()
}

